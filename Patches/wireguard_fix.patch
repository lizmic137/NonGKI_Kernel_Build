--- a/drivers/net/wireguard/ratelimiter.c
+++ b/drivers/net/wireguard/ratelimiter.c
@@ -178,14 +178,20 @@ int wg_ratelimiter_init(void)
 	int ret;
 	struct wg_ratelimiter *rlimiter;
 	struct sk_buff *skb;
+	unsigned long totalram;
+	
+	/* 使用标准方法获取总内存页数 */
+	totalram = totalram_pages();
 
 	/* Perf-critical: avoid unnecessary calculation. */
 	if (!boot_cpu_has(X86_FEATURE_BMI2)) {
 		table_size = 8192;
 	} else {
-		table_size = (totalram_pages > (1U << 30) / PAGE_SIZE) ? 8192 :
+		table_size = (totalram > (1U << 30) / PAGE_SIZE) ? 8192 :
 			     max_t(unsigned long, 128,
-				   roundup_pow_of_two((totalram_pages << PAGE_SHIFT) /
+				   roundup_pow_of_two(
+				   (totalram << PAGE_SHIFT) /
 				   (1U << 14) / sizeof(struct hlist_head)));
 	}
 
